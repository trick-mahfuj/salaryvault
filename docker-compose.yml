# =============================================
# Docker Compose — MNIT Work Ledger
# =============================================
services:
  # --------------------------------------------------
  # App — Next.js production server on port 3000
  # --------------------------------------------------
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: unless-stopped
    env_file:
      - .env.local
